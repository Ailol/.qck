# QckLang is oneliner language <3

```
↺ - loop
& - unstructered
% - pollinated into structure 

time()
condtion()
context()
date()
grain()

time&context&date↺{
    sql(query).mongoDb(queryD)%bitmemory;
}
```
